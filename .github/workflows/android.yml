name: Build Android App

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        
    - name: Setup Android SDK
      uses: android-actions/setup-android@v3
      
    - name: Build APK
      run: |
        chmod +x gradlew
        ./gradlew assembleRelease
        
    - name: Find and upload APK
      run: |
        # Trouver tous les APK
        APK_FILE=$(find . -name "*.apk" -type f | grep release | head -1)
        echo "APK found: $APK_FILE"
        
        # Renommer et déplacer pour upload facile
        if [ -n "$APK_FILE" ]; then
          cp "$APK_FILE" ./z-calculator-app.apk
          echo "APK ready for upload: z-calculator-app.apk"
        else
          echo "No APK file found!"
          find . -name "*.apk" -type f
        fi
        
    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: z-calculator-app
        path: z-calculator-app.apk
        retention-days: 30
